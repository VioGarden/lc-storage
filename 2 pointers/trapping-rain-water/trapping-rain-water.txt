1. Lots of memory : O(n) time and O(n) memory
    -Create list of maximum values from left, starting with 0
    -Create list of maximum values from right, starting from 0
    -get list of min of left and right
    -pos difference between minlr[i] - height[i] is 1 block (iterate, get, add)

2. Pointers, O(n) time and O(1) memory


